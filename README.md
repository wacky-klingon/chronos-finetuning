# chronos-finetuning
Chronos Bolt Offline Fine-Tuner is an open-source project that downloads the Chronos Bolt forecasting model, fine-tunes it on custom time-series data, and saves a fully self-contained offline package for local inference. It supports CPU/GPU runs, reproducible training, CLI tools, and private use in secure or air-gapped environments.
